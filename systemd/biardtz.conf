# /etc/biardtz/biardtz.conf — Extra CLI options for biardtz systemd service.
# Loaded via EnvironmentFile in biardtz.service.
# Edit this file, then: make restart (or: sudo systemctl restart biardtz)
#
# Examples:
#   BIARDTZ_OPTS=--watchlist-file /mnt/ssd/watchlist.txt --auto-watchlist 3
#   BIARDTZ_OPTS=--watchlist "Common Nightingale,Common Cuckoo" --verify-count 2 --verify-window 300

BIARDTZ_OPTS=
